{
  "name": "seo-saas",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "file-saver": "^2.0.5",
    "react-markdown": "^10.1.0"
  }
}
